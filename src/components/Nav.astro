---
import { person } from '../data/profile';

const path = Astro.url.pathname;
const links = [
  { href: '/#work', label: 'projects', current: path.startsWith('/projects/') },
  { href: '/#experience', label: 'experience', current: false },
  { href: '/resume/', label: 'résumé', current: path === '/resume/' },
  { href: person.github, label: 'github', external: true, current: false },
];
---

<header class="nav">
  <div class="container nav-inner">
    <a class="brand" href="/">samuel chen</a>
    <nav aria-label="Primary">
      <ul>
        {links.map((l) => (
          <li>
            <a
              href={l.href}
              aria-current={l.current ? 'page' : undefined}
              {...(l.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
            >{l.label}</a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<style>
  .nav {
    position: sticky; top: 0; z-index: 10;
    background: var(--bg);
    border-bottom: 1px solid var(--border);
  }
  .nav-inner { display: flex; align-items: center; justify-content: space-between; height: 52px; }
  .brand, nav a { font-family: var(--font-mono); font-size: 0.85rem; color: var(--fg); }
  .brand { font-weight: 600; }
  nav { display: flex; align-items: center; gap: var(--space-2); }
  ul { list-style: none; margin: 0; padding: 0; display: flex; gap: var(--space-2); }
  li { margin: 0; }
  nav a { color: var(--fg-muted); transition: color var(--dur) ease; }
  nav a:hover, nav a[aria-current='page'] { color: var(--fg); text-decoration: none; }
  @media (max-width: 767px) { ul { gap: 12px; } nav a, .brand { font-size: 0.8rem; } }
  @media (max-width: 480px) {
    .nav-inner { flex-wrap: wrap; height: auto; padding-top: 10px; padding-bottom: 10px; row-gap: 8px; }
    .brand { white-space: nowrap; }
    nav { width: 100%; justify-content: space-between; }
    ul { gap: 14px; flex-wrap: wrap; }
  }
</style>
